<%- include('../partials/layout.ejs') %>

<div class="container mt-4">
  <h1>Thêm liên lạc</h1>
  <form action="/contacts" method="POST" id="form_contact">
    <!-- DATA -->
    <div class="form-group mb-3">
      <label for="dataContact[NAME]">Nhập họ và tên <span class="text-danger">(*)</span></label>
      <input
        type="text"
        class="form-control"
        id="dataContact[NAME]"
        name="dataContact[NAME]"
        placeholder="Nhập tên..."
        required
      />
    </div>
    <div class="form-group mb-3">
      <label for="dataContact[PHONE]">Nhập số điện thoại </label>
      <input
        type="text"
        class="form-control"
        id="dataContact[PHONE]"
        name="dataContact[PHONE]"
        placeholder="Nhập số điện thoại..."
      />
    </div>
    <div class="form-group mb-3">
      <label for="dataContact[EMAIL]">Nhập địa chỉ email</label>
      <input
        type="email"
        class="form-control"
        id="dataContact[EMAIL]"
        name="dataContact[EMAIL]"
        placeholder="Nhập tên..."
      />
    </div>
    <div class="form-group mb-3">
      <label for="dataContact[WEB]">Nhập địa chỉ web </label>
      <input type="text" class="form-control" id="dataContact[WEB]" name="dataContact[WEB]" placeholder="Nhập tên..." />
    </div>

    <!-- ADDRESS -->
    <div class="row">
      <div class="form-group col mb-3">
        <label for="dataAddress[ADDRESS_1]">Phường/xã</label>
        <input
          type="text"
          class="form-control"
          id="dataAddress[ADDRESS_1]"
          name="dataAddress[ADDRESS_1]"
          placeholder="Nhập phường xã..."
        />
      </div>
      <div class="form-group col mb-3">
        <label for="dataAddress[REGION]">Quận/huyện</label>
        <input
          type="text"
          class="form-control"
          id="dataAddress[REGION]"
          name="dataAddress[REGION]"
          placeholder="Nhập quận/huyện..."
        />
      </div>

      <div class="form-group col mb-3">
        <label for="dataAddress[CITY]">Tỉnh/thành phố</label>
        <input
          type="text"
          class="form-control"
          id="dataAddress[CITY]"
          name="dataAddress[CITY]"
          placeholder="Nhập tỉnh/thành phố..."
        />
      </div>
    </div>

    <div class="form-group mb-3">
      <label for="dataAddress[COUNTRY]">Quốc gia</label>
      <input
        type="text"
        class="form-control"
        id="dataAddress[COUNTRY]"
        name="dataAddress[COUNTRY]"
        placeholder="Nhập quốc gia..."
      />
    </div>

    <!-- Bank -->

    <div class="form-group mb-3">
      <label for="dataBank[RQ_BANK_NAME]">Tên ngân hàng</label>
      <input
        type="text"
        class="form-control"
        id="dataBank[RQ_BANK_NAME]"
        name="dataBank[RQ_BANK_NAME]"
        placeholder="Nhập tên ngân hàng..."
      />
    </div>
    <div class="form-group mb-3">
      <label for="dataBank[RQ_BANK_ADDR]">Địa chỉ</label>
      <input
        type="text"
        class="form-control"
        id="dataBank[RQ_BANK_ADDR]"
        name="dataBank[RQ_BANK_ADDR]"
        placeholder="Nhập địa chỉ ngân hàng..."
      />
    </div>
    <div class="form-group mb-3">
      <label for="dataBank[RQ_ACC_NUM]">Số tài khoản</label>
      <input
        type="text"
        class="form-control"
        id="dataBank[RQ_ACC_NUM]"
        name="dataBank[RQ_ACC_NUM]"
        placeholder="Nhập số tài khoản..."
      />
    </div>

    <div class="mt-3">
      <button type="submit" class="btn btn-primary">Hoàn tất</button>
      <a href="/contacts" class="btn btn-secondary">Cancel</a>
    </div>
  </form>
</div>
